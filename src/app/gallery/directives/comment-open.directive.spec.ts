import { CommentOpenDirective } from './comment-open.directive';

describe('CommentOpenDirective', () => {
  it('should create an instance', () => {
    const directive = new CommentOpenDirective();
    expect(directive).toBeTruthy();
  });
});
