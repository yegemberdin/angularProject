import { ImageBoxShadowDirective } from './image-box-shadow.directive';

describe('ImageBoxShadowDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageBoxShadowDirective();
    expect(directive).toBeTruthy();
  });
});
